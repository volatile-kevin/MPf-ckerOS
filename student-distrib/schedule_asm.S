.globl save_registers


save_registers:
   # pusha
   # pushf 

   # movl %cr3, %eax # save cr3 
   # # push %eax 

   #  mov 8(%ebp), %eax # the location of the first member of the tss struct
   #  mov %esp, 4(%eax) # save esp 
   # mov %

    